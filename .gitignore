node_modules/
dist/
dist-installer/
electron/dist/
*.tsbuildinfo
.DS_Store
.env
.env.local
*.log

# Signing keys must never be committed
build/signing.key
build/*.pfx
build/*.p12

# Local databases
*.db
*.db-wal
*.db-shm

# Signing key — never commit
build/signing.key
dist-installer/
node_modules/
